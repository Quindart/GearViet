{
    "builds": [
      {
        "src": "admin/package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "admin/build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/admin/$1"
      }
    ]
  }
  